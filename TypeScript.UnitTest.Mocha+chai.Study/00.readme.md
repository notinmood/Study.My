# 说明：
▌TypeScript项目的单元测试，不推荐使用Mocha+Chai，推荐使用Vitest。

✅ 需要在终端窗口执行命令：`pnpm test`，以运行测试用例。
❌ 请不要在 IDE 的代码编辑器中运行测试用例，因为 IDE 可能无法正确配置环境变量。

## 配置信息

参考本项目的配置文件`package.json`中：
- `scripts`节点的`test`命令，用于运行测试用例。
- `devDependencies` 节点中的各种依赖，缺少任何一个依赖都会导致测试用例无法运行。